#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<pair<int,int>> input(n);
    for (int i = 0; i < n; ++i)
    {
        cin >> input[i].first >> input[i].second;
    }

    

}